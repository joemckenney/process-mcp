// Tool modules will land here, one per tool. Initial roadmap:
//
//   pids_in_cgroup   given a cgroup path, list PIDs inside it
//   top_processes    rank PIDs by memory (default) or CPU
//   process_info     single-PID drill-down
//   process_tree     parent/child forest (phase 2)
